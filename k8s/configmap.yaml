apiVersion: v1
kind: ConfigMap
metadata:
  name: production-platform-config
  namespace: production-platform
data:
  APP_NAME: "production-kubernetes-platform"
  APP_VERSION: "1.0.0"
  ENVIRONMENT: "kubernetes"
  LOG_LEVEL: "INFO"
  PORT: "8080"
